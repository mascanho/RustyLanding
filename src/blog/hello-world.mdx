export default function BlogContent() {
  return (
    <>


      <p>Welcome to our blog! This is first post.</p>

      <h2 id="what-is-mdx">What is MDX?</h2>

      <p>MDX is a format that allows you to write JSX in Markdown documents. You can import components and use them directly in your content.</p>

      <h2 id="code-example">Code Example</h2>

      <pre><code className="language-jsx">{`function HelloWorld() {
  return <h1>Hello World!</h1>;
}`}</code></pre>

      <p>That's it for now!</p>
    </>
  );
}